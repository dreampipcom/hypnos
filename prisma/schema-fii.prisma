// to-do: schema-fii.prisma
generator client {
  provider = "prisma-client-js"
  output = "../node_modules/@dreampipcom/db-fii/prisma-client"
}

datasource db {
  provider        = "mongodb"
  url             = env("PRISMA_FII_URI")
  directUrl       = env("MONGODB_FII_URI")
}